asyncpg==0.29.0
libsql==0.1.11
fastapi==0.115.6
groq
pydantic-settings==2.7.1
python-dotenv==1.0.1
tzdata==2026.3
# The desktop sidecar serves local REST over HTTP/1.1.  It intentionally does
# not expose WebSocket routes, so avoid Uvicorn's optional `standard` extras
# (including the native websockets speedups extension) in public bundles.
uvicorn==0.34.0
